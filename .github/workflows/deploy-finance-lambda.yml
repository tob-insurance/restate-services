name: Deploy Finance Lambda

on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment to deploy"
        required: true
        default: "prod"
        type: choice
        options:
          - dev
          - staging
          - prod
  push:
    branches: [main]
    paths:
      - "apps/finance/**"
      - "packages/oracle/**"
      - "packages/postgres/**"
      - ".github/workflows/deploy-restate-lambda.yml"

jobs:
  deploy:
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/deploy-restate-lambda.yml
    with:
      app-name: "finance"
      lambda-name: "finance-closing"
      environment: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.environment || 'prod' }}
      oracle-layer-name: "oracle-instantclient"
    secrets: inherit
