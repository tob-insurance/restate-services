{
  "name": "@restate-tob/finance",
  "version": "0.0.1",
  "description": "Finance closing workflow service running with Restate",
  "main": "dist/app.local.js",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmitOnError",
    "test": "bun test src/",
    "typecheck": "tsc --noEmit",
    "lint": "ultracite check",
    "lint:fix": "ultracite fix",
    "start": "node ./dist/app.local.js",
    "dev": "tsx watch ./src/app.local.ts",
    "bundle": "tsdown src/app.local.ts --minify --sourcemap --platform node --target node22 --outDir dist --clean --no-dts",
    "postbundle": "cd dist && zip -r index.zip app.local.mjs*",
    "bundle:lambda": "tsdown",
    "postbundle:lambda": "cd dist-lambda && zip -r lambda.zip app.mjs*",
    "clean": "rm -rf dist dist-lambda"
  },
  "dependencies": {
    "@restate-tob/postgres": "workspace:*",
    "@restate-tob/shared": "workspace:*",
    "@restatedev/restate-sdk": "^1.14.4",
    "luxon": "^3.7.2",
    "pg": "^8.21.0",
    "pino": "^10.3.1",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/luxon": "^3.7.1",
    "@types/node": "^20.14.2",
    "@types/pg": "^8.20.0",
    "dotenv": "^17.4.2",
    "tsdown": "^0.22.0",
    "tsx": "^4.22.3",
    "typescript": "^6.0.3"
  }
}
